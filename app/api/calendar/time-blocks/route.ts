import { NextResponse } from 'next/server';

// GET /api/calendar/time-blocks - Example endpoint
export async function GET() {
	return NextResponse.json({ message: 'Calendar time blocks endpoint is under construction.' });
}

// POST /api/calendar/time-blocks - Example endpoint
export async function POST() {
	return NextResponse.json({ message: 'POST to calendar time blocks is under construction.' });
}
