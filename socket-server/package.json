{
  "name": "chat-socket-server",
  "version": "1.0.0",
  "description": "Real-time chat socket server for workspace communication",
  "main": "server.ts",
  "scripts": {
    "start": "node dist/server.js",
    "dev": "tsx watch server.ts",
    "build": "tsc server.ts --outDir dist",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "dotenv": "^17.2.1",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "prisma": "^5.22.0",
    "tsx": "^4.20.3",
    "typescript": "^5.3.3"
  }
}
